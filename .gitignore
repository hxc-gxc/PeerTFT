# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# Melos
.melos_tool/

# Flutter platform folders (generated by `flutter create .`, see README)
packages/app/ios/
packages/app/web/
packages/app/macos/
packages/app/linux/
packages/app/windows/

# Entitlements are hand-edited, not regenerated — track them
!packages/app/macos/Runner/*.entitlements

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Secrets
.env.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
